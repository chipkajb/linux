#!/usr/bin/env bash
# jump to the i3 workspace for dunst's most recent notification (any urgency)

set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$script_dir/i3-notification-workspace.sh" history
